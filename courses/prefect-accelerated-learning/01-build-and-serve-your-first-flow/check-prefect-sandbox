#!/bin/bash
set -euxo pipefail

cd /root/pal

test -f weather_service.py
grep -q "@flow" weather_service.py
grep -q "fetch_weather" weather_service.py
grep -q "\.serve(" weather_service.py
grep -Fq 'cron="*/5 * * * *"' weather_service.py
